# حل مشكلة "Failed to Fetch"

إذا كنت تواجه خطأ "Failed to Fetch" عند محاولة الاتصال بالباك إند، اتبع الخطوات التالية:

## 1. إنشاء ملف `.env` في الفرونت إند

في مجلد `cms-1.0`، أنشئ ملف `.env` وأضف:

```env
VITE_API_URL=http://localhost:8000/api/v1
```

**ملاحظة مهمة**: بعد إنشاء ملف `.env`، يجب إعادة تشغيل خادم التطوير (`npm run dev`).

## 2. التأكد من أن الباك إند يعمل

افتح terminal جديد وتأكد من أن الباك إند يعمل:

```powershell
cd horizon-cms1.0\BE-1.0
php artisan serve --host=localhost --port=8000
```

يجب أن ترى رسالة:
```
INFO  Server running on [http://localhost:8000].
```

## 3. التحقق من إعدادات CORS

تأكد من أن ملف `BE-1.0/config/cors.php` يحتوي على:

```php
'allowed_origins' => explode(',', env('CORS_ALLOWED_ORIGINS', 'http://localhost:8080,http://localhost:5173,http://localhost:3000')),
```

## 4. التحقق من الاتصال

افتح المتصفح واذهب إلى:
- الباك إند: http://localhost:8000/api/v1/auth/me (يجب أن يعطي رد JSON)
- الفرونت إند: http://localhost:8080

## 5. فتح Console في المتصفح

افتح Developer Tools (F12) واذهب إلى Console. يجب أن ترى:
- `🔗 API Base URL: http://localhost:8000/api/v1` عند تحميل الصفحة
- أي أخطاء مفصلة عند حدوث مشكلة في الاتصال

## 6. المشاكل الشائعة وحلولها

### المشكلة: الباك إند لا يستجيب
**الحل**: تأكد من أن `php artisan serve` يعمل على port 8000

### المشكلة: CORS Error
**الحل**: تأكد من أن `CORS_ALLOWED_ORIGINS` في `.env` الباك إند يحتوي على `http://localhost:8080`

### المشكلة: ملف `.env` غير موجود في الفرونت إند
**الحل**: أنشئ ملف `.env` في `cms-1.0` وأضف `VITE_API_URL=http://localhost:8000/api/v1`

### المشكلة: الباك إند يعمل على 127.0.0.1 بدلاً من localhost
**الحل**: استخدم `php artisan serve --host=localhost --port=8000` بدلاً من العنوان الافتراضي

## 7. إعادة تشغيل الخوادم

بعد إجراء أي تغييرات:

1. أوقف الباك إند (Ctrl+C)
2. أوقف الفرونت إند (Ctrl+C)
3. أعد تشغيل الباك إند: `php artisan serve --host=localhost --port=8000`
4. أعد تشغيل الفرونت إند: `npm run dev`

