# حل مشكلة "Failed to Fetch" - دليل سريع

## المشكلة
عند محاولة تسجيل الدخول أو أي عملية أخرى، تظهر رسالة خطأ: **"Failed to Fetch"**

## الحل السريع

### 1. إنشاء ملف `.env` في الفرونت إند

في مجلد `cms-1.0`، أنشئ ملف جديد باسم `.env` وأضف فيه:

```
VITE_API_URL=http://localhost:8000/api/v1
```

**⚠️ مهم جداً**: بعد إنشاء الملف، أوقف خادم التطوير (Ctrl+C) وأعد تشغيله:
```powershell
npm run dev
```

### 2. تأكد من أن الباك إند يعمل

افتح terminal جديد (PowerShell) وقم بتشغيل:

```powershell
cd horizon-cms1.0\BE-1.0
php artisan serve --host=localhost --port=8000
```

يجب أن ترى:
```
INFO  Server running on [http://localhost:8000].
```

### 3. تحقق من CORS

تأكد من أن ملف `BE-1.0/.env` يحتوي على (أو أضفه إذا لم يكن موجوداً):

```env
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000
```

ثم أعد تشغيل الباك إند.

### 4. افتح Console في المتصفح

اضغط F12 في المتصفح، واذهب إلى تبويب Console. يجب أن ترى:
- `🔗 API Base URL: http://localhost:8000/api/v1` عند تحميل الصفحة
- أي أخطاء مفصلة عند حدوث مشكلة

## خطوات الاختبار

1. ✅ افتح http://localhost:8000/api/v1/auth/me في المتصفح
   - يجب أن ترى رد JSON (حتى لو كان خطأ auth)

2. ✅ افتح http://localhost:8080 في المتصفح
   - يجب أن يعمل الفرونت إند

3. ✅ حاول تسجيل الدخول
   - يجب أن تعمل بدون خطأ "Failed to Fetch"

## إذا استمرت المشكلة

### تحقق من:
- ✅ الباك إند يعمل على http://localhost:8000 (وليس 127.0.0.1)
- ✅ الفرونت إند يعمل على http://localhost:8080
- ✅ ملف `.env` موجود في `cms-1.0` ويحتوي على `VITE_API_URL`
- ✅ أعدت تشغيل `npm run dev` بعد إنشاء `.env`
- ✅ لا توجد جدار ناري يمنع الاتصال

### في Console المتصفح، تحقق من:
- الـ Network tab: هل الطلبات تذهب إلى `localhost:8000`؟
- الـ Console tab: هل هناك أخطاء CORS أو network errors؟

## مثال على `.env` صحيح

**ملف `cms-1.0/.env`:**
```
VITE_API_URL=http://localhost:8000/api/v1
```

**ملف `BE-1.0/.env` (يجب أن يحتوي على):**
```
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000
```

---

**💡 نصيحة**: بعد أي تغيير في ملفات `.env`، يجب إعادة تشغيل الخوادم.

